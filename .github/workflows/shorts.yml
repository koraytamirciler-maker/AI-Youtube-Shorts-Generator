name: Shorts Generator

on:
  workflow_dispatch:
    inputs:
      topic:
        description: "Video konusu"
        required: true
        default: "Yapay zeka"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Kullanicinin girdigi konuyu goster
        run: echo "Konu: ${{ github.event.inputs.topic }}"
